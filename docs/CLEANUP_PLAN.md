# План очистки проекта до «чистого старта»

Цель: привести репозиторий к состоянию «начало новой фазы» — убрать мусор, исправить риски и зафиксировать единые правила работы. Выполнять по порядку.

---

## 1. Безопасность и секреты (критично)

| #   | Действие                                                                             | Статус |
| --- | ------------------------------------------------------------------------------------ | ------ |
| 1.1 | Удалить из индекса Git (не удаляя файлы): `.env`, `.env.local`, `packages/db/.env`   | ✅     |
| 1.2 | Создать `.env.example` в корне и в `packages/db/` с перечнем переменных без значений | ✅     |
| 1.3 | Убедиться, что `.gitignore` игнорирует `.env`, `.env.*` (кроме `!.env.example`)      | ✅     |

Команды для 1.1:

```bash
git rm --cached .env .env.local packages/db/.env 2>/dev/null; true
```

---

## 2. Корень проекта — разовый «мусор»

Файлы в корне, которые не должны жить в репозитории как рабочие артефакты:

| Файл                                  | Действие                                             |
| ------------------------------------- | ---------------------------------------------------- |
| `ADD_COLORS_COLUMNS.sql`              | Переместить в `docs/archive/` или `scripts/archive/` |
| `MIGRATE_ATTRIBUTE_VALUES_COLORS.sql` | То же                                                |
| `QUICK_FIX_COLORS.sql`                | То же                                                |
| `QUICK_FIX_MIGRATION.sql`             | То же                                                |
| `run-migration.sql`                   | То же (или привязать к конкретной миграции в README) |
| `fix-colors-error.ps1`                | В `scripts/archive/` или удалить, если не нужен      |
| `fix-prisma-client.ps1`               | В `scripts/archive/` или удалить                     |

**Выполнено:** SQL и PS1 перенесены в `docs/archive/` и `scripts/archive/`.

Итог: в корне остаются только конфиги (`package.json`, `turbo.json`, `vercel.json`, `.eslintrc.js`, `.prettierrc`, `.editorconfig`, `.gitignore`), `LICENSE`, `README.md`, папки `app/`, `apps/`, `packages/`, `config/`, `scripts/`, `docs/`, `reference/`, `.cursor/`, `.github/`.

---

## 3. Папка `scripts/` — упорядочивание

| Категория            | Скрипты                                                                                                                                                                                                                                                                                     | Действие                                                                                    |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| Оставить как утилиты | `create-admin.ts`, `deploy-neon-migrations.ts`, `setup-neon-db.ts`                                                                                                                                                                                                                          | Оставить, проверить актуальность                                                            |
| Миграции/одноразовые | `migrate-mongo-to-postgres.ts`, `migrate-db-to-utf8.ts`, `migrate-variants-to-attributes.ts`, `run-attribute-value-migration.ts`, `add-colors-column.ts`, `add-colors-columns.ts`, `add-order-quantity-fields.ts`, `add-sample-products.ts`, `add-10-new-products.ts`, `add-15-products.ts` | Переместить в `scripts/archive/` с кратким README, что это исторические/одноразовые скрипты |
| Фиксы/проверки       | `check-db-encoding.ts`, `fix-database-encoding.sql`, `fix-database-encoding-to-utf8.sql`                                                                                                                                                                                                    | В `scripts/archive/`                                                                        |
| Тесты/валидация      | `test-api-routes.js`, `validate-routes.js`                                                                                                                                                                                                                                                  | Оставить или перенести в тесты (Vitest)                                                     |
| Устаревшее           | `cleanup-old-backend.js`                                                                                                                                                                                                                                                                    | В `scripts/archive/` или удалить                                                            |
| Документация         | `NEON_MIGRATION_README.md`                                                                                                                                                                                                                                                                  | Оставить в `scripts/` или перенести в `docs/`                                               |

Создать `scripts/README.md`: что за скрипты остаются, как их запускать, что лежит в `archive/`.

---

## 4. Миграции БД — единый источник правды

| #   | Действие                                                                                    | Статус                 |
| --- | ------------------------------------------------------------------------------------------- | ---------------------- |
| 4.1 | Официальные миграции — только в `packages/db/prisma/migrations/`                            | ✅                     |
| 4.2 | Все разовые SQL из корня и `scripts/` — в архив, в коде ссылаться только на Prisma-миграции | ✅                     |
| 4.3 | В `packages/db/` или в `docs/` кратко описать, как запускать миграции (Neon, локально)      | ➖ (README в scripts/) |

---

## 5. Зависимости и сборка

| #   | Действие                                                                                                | Статус |
| --- | ------------------------------------------------------------------------------------------------------- | ------ |
| 5.1 | Проверить `package.json`: удалить неиспользуемые зависимости (например, `mongoose`, если всё на Prisma) | ⬜     |
| 5.2 | `npm install` / `pnpm install` — убедиться, что сборка и скрипты работают                               | ⬜     |
| 5.3 | В `.gitignore` учтены `node_modules/`, `apps/web/.next/`, `.turbo/`                                     | ✅     |

---

## 6. Документация и онбординг

| #   | Действие                                                                                                             | Статус |
| --- | -------------------------------------------------------------------------------------------------------------------- | ------ |
| 6.1 | Заполнить `docs/BRIEF.md` под проект borboraqua.am (описание, функции, интеграции)                                   | ✅     |
| 6.2 | Создать `docs/TECH_CARD.md` по шаблону `reference/templates/TECH_CARD_TEMPLATE.md` и заполнить по текущему стеку     | ✅     |
| 6.3 | Создать `docs/PROGRESS.md` по шаблону из `reference/templates/PROGRESS_TEMPLATE.md` — текущая фаза «очистка и старт» | ✅     |

---

## 7. Линтеры и формат

| #   | Действие                                                                         | Статус |
| --- | -------------------------------------------------------------------------------- | ------ |
| 7.1 | Запустить `npm run lint` / `pnpm lint` (если есть), исправить критические ошибки | ⬜     |
| 7.2 | Prettier по проекту (по `.prettierrc`), зафиксировать изменения                  | ⬜     |
| 7.3 | Проверить `.editorconfig` — единый стиль отступов и переносов строк              | ✅     |

---

## 8. Финальный коммит и тег

| #   | Действие                                                                           | Статус |
| --- | ---------------------------------------------------------------------------------- | ------ |
| 8.1 | После выполнения 1–7: один коммит «chore: cleanup to project start (CLEANUP_PLAN)» | 🔄     |
| 8.2 | При необходимости — новый тег, например `phase-start-clean`                        | ➖     |

---

## Порядок выполнения (кратко)

1. Секреты: убрать `.env*` из Git, добавить `.env.example`.
2. Корень: перенести разовые SQL и `.ps1` в архив.
3. Scripts: разнести по «активные» и `scripts/archive/`, добавить README.
4. Миграции: только Prisma, остальное — в архив и в документацию.
5. Зависимости: почистить, проверить сборку.
6. Документация: BRIEF, TECH_CARD, PROGRESS.
7. Линт и формат.
8. Коммит и при необходимости тег.

После этого проект считается в состоянии «чистый старт» для продолжения разработки по правилам из `.cursor/rules/` и [Rules-Template](https://github.com/neetrino/-Rules-Template).
